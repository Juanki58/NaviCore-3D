# Allan smoke artefacts

**Not a published IMU datasheet.** Synthetic 60 s CSV from `tools/generate_imu_static_smoke.py` only proves `analyze_allan.py` wiring.

For the Evidence scorecard ARW/BI table, follow [`RUNBOOK.md`](../RUNBOOK.md) with a multi-hour `docs/imu_static_log.csv` from a real DUT.
